class Program
{
    static void Main()
    {
        try
        {
            int a = 10;
            int b = 0;
            int c = a / b;
        }
        catch (DivideByZeroException e)
        {
            Console.WriteLine("Произошла ошибка деления на ноль: {0}", e.Message);
        }
    }
}
