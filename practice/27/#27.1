using System.Collections;

class Program
{
    public static void Main()
    {
        List<object> myList = new();
        myList.Add(2024);
        myList.Add(" Alif Academy ");
        myList.Add('C');
        myList.Add('#');
        myList.Add(" Course");

        foreach(var i in myList)
        {
            Console.Write(i);
        } 
    }
}
