using System;

class Program
{
    static void Main()
    {
        int age = 21;
        bool isStudent = false;

        if (age >= 18)
        {
            isStudent = true;
            // Console.WriteLine(isStudent);
            Console.WriteLine("Ты являешься студентом");
        }
        else
        {
            isStudent = false;
            // Console.WriteLine(isStudent);
            Console.WriteLine("К сожалению (или к счастью), ты не являешься студентом");
        }
    }
}
